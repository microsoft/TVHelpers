<controls:ViewHeaderBase
    x:Class="MediaAppSample.UI.Controls.ViewHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MediaAppSample.UI.Controls"
    xmlns:triggers="using:MediaAppSample.UI.Triggers"
    xmlns:designTime="using:MediaAppSample.Core.ViewModels.Designer"
    d:DataContext="{d:DesignInstance designTime:MainViewModel, IsDesignTimeCreatable=True}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="userControl"
    VerticalAlignment="Top"
    d:DesignHeight="600"
    d:DesignWidth="600"
    mc:Ignorable="d">

    <Grid Background="Transparent">
        
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="MobileLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="btnSearch.Visibility" Value="Collapsed" />
                        <Setter Target="btnSearchCompact.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideLayout">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="640" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="btnSearch.Visibility" Value="Visible" />
                        <Setter Target="btnSearchCompact.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.ColumnSpan="3" Background="{ThemeResource ThemeHubHeaderBackground}" />

        <TextBlock Text="{Binding ViewModel.Title, Mode=OneWay, FallbackValue='N/A'}" VerticalAlignment="Center" />
        <Image Source="/Assets/HeaderLogo_Full.png" Height="48" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="{Binding ViewModel.Title, Converter={StaticResource visInvertedConverter}}"/>

        <AppBarButton Grid.Column="1" Icon="Refresh" Command="{Binding ViewModel.RefreshCommand, Mode=OneWay}" Visibility="{Binding ViewModel.IsRefreshVisible, Mode=OneWay, Converter={StaticResource visConverter}}" />

        <Grid Grid.Column="2" Visibility="{Binding ViewModel.IsUserAuthenticated, Mode=OneWay, Converter={StaticResource visConverter}}">
            <controls:SearchBox x:Name="btnSearch" VerticalAlignment="Center" Width="200" Height="48" Margin="0,0,12,0"  />
            <AppBarButton x:Name="btnSearchCompact" Icon="Find" Margin="0" Width="48" Background="{ThemeResource ThemeHubHeaderBackground}">
                <AppBarButton.Flyout>
                    <Flyout>
                        <controls:SearchBox MinWidth="250" />
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </Grid>
        
        <controls:ViewHeaderStatus Grid.ColumnSpan="3" VerticalAlignment="Top" />

    </Grid>

</controls:ViewHeaderBase>
