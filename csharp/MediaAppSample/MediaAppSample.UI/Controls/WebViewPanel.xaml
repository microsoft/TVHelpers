<controls:WebViewPanelBase
    x:Class="MediaAppSample.UI.Controls.WebViewPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MediaAppSample.UI.Controls"
    xmlns:designTime="using:MediaAppSample.Core.ViewModels.Designer"
    d:DataContext="{d:DesignInstance designTime:WebBrowserViewModel, IsDesignTimeCreatable=True}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <WebView x:Name="webView" Visibility="{Binding ViewModel.ShowBrowser, Mode=OneWay, Converter={StaticResource visConverter}}" />
        <TextBlock x:Name="tbError"
                       Visibility="{Binding ViewModel.BrowseErrorMessage, Mode=OneWay, Converter={StaticResource visConverter}}"
                       Text="{Binding ViewModel.BrowseErrorMessage, Mode=OneWay}"
                       TextAlignment="Center"
                       VerticalAlignment="Center" />

        <CommandBar Grid.Row="2" Visibility="{Binding ViewModel.ShowNavigation, Mode=OneWay, Converter={StaticResource visConverter}}">
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="Go back" Icon="Back" Command="{Binding ViewModel.Platform.Navigation.NavigateGoBackCommand}" />
                <AppBarButton Label="Home" Icon="Home" Command="{Binding ViewModel.BrowserHomeCommand}" />
                <AppBarButton Label="Refresh" Icon="Refresh" Command="{Binding ViewModel.BrowserRefreshCommand}" />
                <AppBarButton Label="Go forward" Icon="Forward" Command="{Binding ViewModel.Platform.Navigation.NavigateGoForwardCommand}" />
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Grid>
</controls:WebViewPanelBase>
