<views:MainViewBase
    x:Name="pageRoot"
    x:Class="MediaAppSample.UI.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:MediaAppSample.UI.Views"
    xmlns:viewmodels="using:MediaAppSample.Core.ViewModels"
    xmlns:models="using:MediaAppSample.Core.Models"
    xmlns:controls="using:MediaAppSample.UI.Controls"
    xmlns:behaviors="using:MediaAppSample.UI.Behaviors"
    xmlns:designTime="using:MediaAppSample.Core.ViewModels.Designer"
    d:DataContext="{d:DesignInstance designTime:MainViewModel, IsDesignTimeCreatable=True}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>

        <DataTemplate x:Name="HeroImageDataTemplate" x:DataType="models:ContentItemBase">
            <Grid>
                <Image Source="{x:Bind FeaturedImage, Mode=OneWay}" Stretch="UniformToFill" Opacity=".3" />
                <StackPanel HorizontalAlignment="Left" VerticalAlignment="Bottom" MaxWidth="400">
                    <TextBlock Text="{Binding Flag}" />
                    <TextBlock Text="{x:Bind Title}" Style="{StaticResource TitleTextBlockStyle}" />
                    <TextBlock Text="{x:Bind Description}" />
                    <ProgressBar Minimum="0" Maximum="100" Value="80" />
                </StackPanel>
            </Grid>
        </DataTemplate>

    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Hub Orientation="Vertical">

            <Hub.Header>
                <Grid Height="48" Background="{ThemeResource ThemeHubHeaderBackground}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Source="/Assets/HeaderLogo.png" Height="40" Stretch="UniformToFill" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <TextBlock x:Name="MainHeaderTitle" x:Uid="SimplyEntertaining" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center" />
                </Grid>
            </Hub.Header>

            <!-- Featured Hero -->
            <HubSection Height="{x:Bind ViewModel.DeviceWindowHeight, Mode=OneWay}" DataContext="{x:Bind ViewModel.FeaturedHero, Mode=OneWay}">
                <DataTemplate x:DataType="viewmodels:MainViewModel">
                    <Grid Margin="0">
                        <ContentPresenter ContentTemplate="{StaticResource HeroImageDataTemplate}" />
                        <ListView ItemsSource="{Binding ViewModel.QueueViewModel.QueuePreview, Mode=OneWay}" VerticalAlignment="Bottom" HorizontalAlignment="Right" />
                    </Grid>
                </DataTemplate>
            </HubSection>

            <!-- Movies Featured -->
            <HubSection Height="{Binding ViewModel.DeviceWindowHeight, Mode=OneWay}" Header="Movies">
                <DataTemplate x:DataType="viewmodels:MainViewModel">
                    <ListView ItemsSource="{Binding ViewModel.MoviesFeatured, Mode=OneWay}" />
                </DataTemplate>
            </HubSection>

            <!-- New Released Movies -->
            <HubSection Header="New Released Movies">
                <DataTemplate x:DataType="viewmodels:MainViewModel">
                    <ListView ItemsSource="{Binding ViewModel.MovieNewReleases, Mode=OneWay}" />
                </DataTemplate>
            </HubSection>

            <!-- TV Featured -->
            <HubSection Height="{Binding ViewModel.DeviceWindowHeight, Mode=OneWay}" Header="TV">
                <DataTemplate x:DataType="viewmodels:MainViewModel">
                    <ListView ItemsSource="{Binding ViewModel.TvFeatured, Mode=OneWay}" />
                </DataTemplate>
            </HubSection>

            <!-- New TV Episodes -->
            <HubSection Header="New Episodes">
                <DataTemplate x:DataType="viewmodels:MainViewModel">
                    <Grid Background="#FF1F1F1F">
                        <ListView ItemsSource="{Binding ViewModel.TvNewReleases, Mode=OneWay}" />
                    </Grid>
                </DataTemplate>
            </HubSection>

            <!-- TV sneak peeks -->
            <HubSection Header="Sneak Peeks">
                <DataTemplate x:DataType="viewmodels:MainViewModel">
                    <ListView ItemsSource="{Binding ViewModel.TvInline, Mode=OneWay}"
                              IsItemClickEnabled="True" />
                </DataTemplate>
            </HubSection>

        </Hub>
        
    </Grid>
    
</views:MainViewBase>
